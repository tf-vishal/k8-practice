kind: Service
apiVersion: v1
metadata:
  name: apache-svc
  namespace: apache
spec:
  selector:
      app: apache
  ports:
    - protocol: TCP
      port: 80 # Exposed port in the cluster
      targetPort: 80 # COntainer Port
  type: ClusterIP
